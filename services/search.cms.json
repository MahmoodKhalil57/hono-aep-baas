{
  "config": {
    "embeddings": {
      "model": "@cf/baai/bge-m3",
      "provider": "workers-ai"
    }
  },
  "description": "Search over collection rows: lexical everywhere; SEMANTIC + hybrid (RRF) on Workers via @cf/baai/bge-m3 (the AI binding provides the embedder). Local Bun has no embedder \u2192 lexical only.",
  "kind": "search",
  "provider": "local"
}
