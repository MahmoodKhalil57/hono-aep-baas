# Commerce — moved

This spec now lives with the package that implements it:
**`customPackages/hono-aep-commerce/spec/commerce.md`**.

It was authored here before `hono-aep-commerce` was indexed in the suite
manifest; every `TODO(commerce)` in the suite points at its content, so it
belongs beside the code. Specs referencing `commerce.md` resolve to the new
location.
